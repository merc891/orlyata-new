# Карта компонентов

Пути ниже — целевая архитектура темы. Для реализованных компонентов путь указывает на существующий production-файл; для остальных — на будущего владельца.

| Figma | Будущий компонент | Варианты/состояния из Figma | Обязательные дополнения |
| --- | --- | --- | --- |
| `Button` `4651:1140` | `template-parts/components/button.php` — реализован | primary/secondary, play, arrows, gallery arrows (Neutral 50 → Neutral 150 hover), `menu-tablet`, mobile `archive-filter` size modifier; default/hover/active/disabled/loading в витрине, реальный keyboard focus-visible; закрытый `menu-tablet` в Sidebar на tablet/mobile использует Untitled UI Menu-05, сохраняет 48 px target и glyph 24 px / stroke 2, а открытый morph-ится в прежний X вместе с `aria-expanded` | mobile-вариант реализован |
| `Badge` `4651:1187`; About life `4903:1700` | `badge.php` — реализован | default, `filled` (borderless `color-surface-muted`, `color-text-secondary-on-muted`), `icon`, inverse поверх изображения, `dark` и `dark-icon` для «Жизни капеллы» (`color-surface-dark` `#403F3F`; dark text — `color-text-inverse`, dark-icon glyph — the existing NewsCard category asset at the same mode-specific size); `icon` — декоративный круг без границы с фоном `color-surface-muted`, 64×64 px на desktop 1920/tablet 1279 и 48×48 px на mobile 393; text Badge на mobile 393 использует 14px/1.2/0 с 12px inline padding | категории публикаций, selected filter, focus при интерактивности |
| `Link` `4651:1238` | `text-link.php` — реализован | roll, color, color-inverse; default/hover/active; `has_chevron` appends decorative Lucide `chevron-right` from Morphicons, 24×24 px with `space.1` (4 px) gap, 1px downward alignment and left-to-right glyph roll on hover/focus | focus-visible, visited policy; в Home mobile-шапках связанных H2 текст скрыт только визуально, остаётся стрелка 24×24 px, а исходная подпись сохраняет доступное имя ссылки |
| `Card news` `4651:1252` | `news-card.php` — реализован | default/hover; заголовок использует роль Body во всех режимах; при pointer hover карточка вырастает на 20 px вверх за 400 ms по `cubic-bezier(0.46, 0, 0, 1)`, glyph и заголовок поднимаются, а нижний Badge с датой остаётся на месте; заголовок прокручивается вертикально, как в MediaCard; date и круглый category Badge в нижней строке прилегают друг к другу без gap во всех режимах. Category map is centralised to reviewed Figma-derived glyph assets: news `4913:696`, announcement `4913:698`, achievement `4913:697`; glyph is 32×32 px at desktop/tablet references and 24×24 px at the 393px mobile reference, following Sₘ down to 320px. desktop сохраняет aspect-ratio карточки и масштабирует её внутренности. В Home tablet carousel-слайд остаётся full-width, а его reference-высота 270 px следует Sₜ. В Home mobile-композиции карточка имеет reference высоту 240 px; обе величины следуют Sₘ и карточка не наследует desktop ratio | category glyph, focus-within, long title, missing content |
| `Advantage` `4651:1331`; Home mobile 393px reference height is 140px with 12px grid gaps | `advantage.php` — реализован | stat, reference 1920 px: 388×170 и padding 24/24/16/24; desktop сохраняет aspect-ratio карточки и масштабирует её внутренности; декоративная точка стоит на inline-start одной вертикали с подписью, центрируется относительно цифры, а цифра начинается после масштабируемого 16 px gap на desktop/tablet и 8 px на mobile. В Home mobile 2×2 grid reference-высота карточки 170 px следует Sₘ | длинные значения, responsive composition |
| `Input` `4651:1477` | `input.php` — реализован | default/typing/filled/error; 64 px, padding 16×2, stacked text без gap, label transition 400ms, ошибки без конечной точки, focus одной brand-границей; date template for focused empty input; empty telephone starts with +7 ( and then formats the entered number without an underlay | disabled, autocomplete, success |
| `SearchInput` | `template-parts/components/search-input.php` — реализован | default/typing/focus-visible/loading; 64 px desktop control, 16 px inline padding, `radius-medium` (16 px reference) in every field state, vertically centred body text and one 1 px brand border during focus/typing; named `archive-control` на tablet получает shared `--button-height` для точного совпадения с соседними filter Button; на «Нотах» input запускает 1000 ms loading через 120 px reference от фильтров с тремя переиспользованными Button-точками color-neutral-200, затем показывает результаты или empty-state; на «Нотах» mobile SearchInput раскрывается page-owned toggle-кнопкой перед filter row и перекрывает её в пределах 12px content inset; tablet и desktop не меняются | empty/error copy follows the separate notes search state contract |
| `Table_row` + `Table_header` | `data-table.php` — реализован | achievements (desktop/tablet: three equal tracks fill the area before global editorial column 3 after their gaps; Year occupies the first, composed Achievement / Choir spans the next two, Competition aligns with the start of global column 3; mobile keeps its existing three-level reading record); teacher-achievements (desktop: Year repeats in columns 1–2 and Achievement begins at 3, spanning 3–4; tablet: Year remains in columns 1–2 and Achievement begins at 3, aligned with the quote below; mobile: same first-line reading record as Home achievements without Competition — Year in the shared leading track, Achievement in the second track after --home-mobile-space-32); news/photo/video: Name spans desktop editorial columns 1–2, Date starts at 3, Type and right-aligned accessible arrow link occupy 4; at desktop/tablet the header matches the data-row height (48px at the reference), centers its text vertically and has a 1px bottom separator; At tablet, News retains its documented natural-height multiline refinement. Photo and Video reuse the standard shared four-track DataTable and visible header without archive-specific geometry overrides. News and Photo mobile use the archive reading record: 16px arrow centered vertically in a 16×24px wrapper with a 1.5px stroke at the 393px reference, primary Body name above the secondary Body date in the second column; a multiline name grows the row naturally, archive-only 24px leading track plus 32px inset (56px total), Home achievements row padding and divider rhythm, a mobile-only --home-mobile-space-2 (2px reference) primary-to-secondary gap, visually hidden Type and a retained 48px link target; Video uses that same record but replaces the arrow with a 24px-at-393px provider SVG icon (YouTube, RuTube or VK), gray by default and colored on hover/focus. Mobile does not display the visual header; 1px нижняя граница каждой data-строки. На mobile `notes` повторяет News reading record: download-стрелка направлена вниз слева, Title / Author — одной primary-строкой, категория хора — secondary ниже; semantic cells and headers remain available. Tablet и desktop сохраняют shared four-track table. На «Нотах» строки, вновь показанные выбором фильтра, получают page-owned opacity 0→1 за 800 ms | семантические таблицы, mobile mapping, empty state, pagination |
| `Accordeon` `4666:741` | `accordion.php` — реализован | closed/opened; одна открытая строка; раскрытие измеряемой panel-обёртки — 400ms `cubic-bezier(0, 0, 0.06, 1)`, декоративный Plus/Minus 2 px morph через `morphicons/dom`; mobile 393px — two-column summary: inline label «группа / возраст» с обычными пробелами вокруг `/` и единой типографикой всех трёх частей, right toggle flush to the row inline end, row 48px, glyph uses the approved mobile icon tokens; About mobile keeps dividers between group rows, omitting only the final divider; opened content begins 8px below summary at 393px | mobile contract implemented; tablet refinement) |
| `Card` `4651:1333`; Mobile 393px: Play shares the Hero button-play.svg glyph at 12px, with 12px image insets; image-to-title and title-to-Badge are 8px | `media-card.php` — реализован | Big сохраняет ratio `797/540`, Small — `390/540` на desktop; image region Small сохраняет desktop-долю `300/540` (`5/9` высоты). На tablet Small — component-owned natural-height stack: image region `13/10`, content region `200px` на reference 1279 через `--media-card-small-tablet-content-height`; title-to-metadata gap одинаков на Главной и внутренних страницах. Заголовок Small использует роль Body во всех режимах, включая mobile; date is the only visible metadata; Big uses a borderless white Surface date Badge at 24 px top/left and video Play directly after it with 0 px gap; Play uses the shared white Surface background except in MediaCard Small, where it uses the borderless color-surface-muted treatment; text and icon Badges use the same borderless color-surface-muted treatment; a desktop/tablet Small video places Play directly after its date Badge with 0 px gap outside the image region; title inset is 24 px inline and 32 px bottom; provider name is never rendered; static image; content/Play above gradient; оболочка, image region, title measure, padding, Badge и внутренние gaps масштабируются вместе. Для Home tablet первых трёх секций утверждено только page-scoped размещение: Big full-row, Small в две колонки. На mobile `320–767 px` Big не используется как отдельный вид: все карточки получают Small-геометрию; date Badge Big остаётся белым без обводки, остальные Badge — залитые surface-muted без обводки и стили заголовка; заголовок Body расположен поверх image region через 16 px от левого и нижнего краёв; его контейнер имеет ширину 310 px на reference 393 px и масштабируется через --media-card-mobile-title-width; нижние 50% изображения покрыты градиентом от transparent до `rgb(0 0 0 / 80%)` под текстом. Именованный вариант `mobile-overlay-meta` размещает залитые Date Badge и 36px Play без обводки в ряд без gap внутри image region: точно на 8 px над заголовком; Play glyph остаётся 16px. Home размещает их одним столбцом с вертикальным gap 24 px на reference `393 px`. Home вправе менять только одобренный span, порядок и потоковое размещение; иной ratio сначала оформляется named variant компонента | photo/video, missing image, focus, long title, responsive visual/geometry checks |
| PageHero | `page-hero.php` — реализован | default (Display H1 at desktop/tablet and Heading 1 H1 at mobile 320–767px); all internal-page variants use `assets/images/teachers/bg-mobile-inner.png` (590×660px, 1.5×) at mobile 320–767px through `picture/source`, with `object-fit: cover` and `object-position: center bottom`, without changing Hero container geometry; tablet/desktop keep their page-specific image and alignment; archive (the same type roles plus the existing top `arrow-left` link to «Медиагалерея» on Photo and Video; on mobile 320–767px its button begins after `--sidebar-mobile-header-height`, exactly like news-detail); news-detail (580px at 1920px, semantic H1 with Heading 1 visual role on desktop/tablet and Heading 2 only on mobile, back Button, inverse metadata, supplied 48px share controls with white surfaces and shared 200ms opacity hover/active. On desktop/tablet the bottom edge of share controls aligns with the H1 bottom edge; on mobile, Back and share align at the top edge below `--sidebar-mobile-header-height`. Meta and H1 retain a 16px reference gap; H1 ends `--space-5` from the Hero bottom. On tablet it belongs to the same shared `Sₜ` token projection as MediaGallery, including the following article and gallery tokens; page CSS does not create a separate Hero or article scaling curve. The mobile Hero uses a minimum height; `--news-detail-mobile-back-meta-gap` preserves 48px at 393px from back Button to meta, so a four-line title naturally grows the image); teacher-detail (580px, top `arrow-left`, inverse Badge at the mirrored top inset, Body description 440px wide and Display H1 after 24px without inheriting that width; a supplied 1.5x foreground portrait is centered over the news-detail background at full Hero height. At 768–1279px it uses the same `news-detail` tablet shell and `Sₜ` projection: the description keeps its token measure, while the title is capped by the available content width and wraps without changing the Hero geometry; at mobile 393px the portrait composition below the shared 72px mobile header is 440px through `--teacher-detail-mobile-hero-height`, so the outer Hero is 512px; its background `teachers/bg-mobile-inner.png` starts below the header. The back Button and 36px inverse Badge align by their top edges, the 340px-wide transparent portrait is centered on the bottom edge, the semantic FIO H1 stays inside the photo area, uses the mobile Heading 1 visual role and wraps to three visual lines. The single CMS-owned 310px Body position is placed in normal flow below the photo area, aligns with the mobile page gutter, uses text-secondary on the white page surface and starts after a 24px gap through `--teacher-detail-mobile-position-gap`; when present, the Achievements section begins 48px below the position copy through page-owned `--teacher-detail-hero-achievements-gap`; all mobile lengths follow Sₘ); optional corner-meta is Body inverse; «Ноты» размещают visible file count at 24 px right/24 px bottom on 1920 and reveal a changed value with opacity 0→1 over 400 ms easing-out; title-reveal; owns `radius-large` by default, while full-bleed page shells may override only outer corners; at mobile 320–767 the default Hero starts at the viewport top behind the shared Sidebar and has a 290px reference height at 393px; title reveals once from opacity 0 and `translateY(1.25rem)` over 400 ms with `easing-home-reveal` | tablet refinement; mobile refinement |
| 404 page empty state | `404.php` — desktop preview | Sidebar + Footer and existing `notes-search-empty.svg`; server-rendered H1/copy; page-owned 400ms opacity/translateY enter identical to the Notes empty state | tablet/mobile composition — stage 6 |

Tablet clarification: default and archive PageHero containers preserve their approved tablet aspect ratio through `--page-hero-height = 355px × S_t`: 355px at the 1279px reference and 213.25px at 768px. Their available width and inner type/insets follow the same shared tablet scope. This applies to About, News, Notes, MediaGallery and Photo/Video archives. Home has its own Hero; news detail, photo-album detail and teacher detail retain their separately scaled Hero contracts.

PageHero tablet media framing clarification: default and archive PageHero variants apply the shared `image-inline-start` media framing only at `768–1279px`, using `object-position: left center` on their supplied background image. It exposes the approved green field at the inline start while leaving their desktop/mobile framing and all detail Hero variants unchanged.

| Teacher card | `teacher-card.php` — реализован | page-owned About card; at 1920 px: `257×340`, portrait `160×160`; mobile 393px: two-column grid with `12px` row and column gaps; cards fill the available width and retain `260px` height, `120×120` portrait with `16px` top inset and `20px` name-block bottom inset; only the explicitly supplied local portrait is rendered, without placeholder/fallback imagery; hover on the full card rolls both text lines with the shared link-roll motion and scales only the portrait to 110% inside its fixed clipping frame with the MediaCard hover timing | real portrait, long name, desktop ratios, hover roll |

About Life tablet clarification: the approved `607.5×520px` card/photo ratio remains component-owned at the 1279px reference. The dark-card padding, tag, copy and arrow group share one `24px × S_t` inline inset through `--about-life-card-padding = --about-life-controls-inset`; this is 24px at 1279px and scales to 14.41px at 768px. Desktop and mobile contracts remain unchanged.
| Form containers `4527:7798`, `4527:7830`, `4527:8773` | `sections/application-form.php` — реализован | home/about; default/submitting/success/validation-error/network-error/server-error; 500×536 px и success 500×456 px; desktop масштабирует целиком форму, включая padding, поля, gaps и фон секции; `novalidate`, submit показывает ошибки только через Input; Interactive Storybook demo локально воспроизводит валидацию → submitting → success | endpoint, nonce/idempotency, anti-spam, юридическое решение по отдельному consent-control, mobile — этапы 6 и 7 |
| `Sidebar` `4641:3718` | `layout/sidebar.php` — реализован | reference 1920 px: 250 px; tablet `768–1279`: shared top header uses `logo-tablet.svg`, `11.25rem` × `6.25625rem` (180 × 100.1 px at 1279), 210 px header CTA at 1279 px that follows S_t down to 768 px and `menu-tablet` after a 4 px gap. Its viewport-height overlay slides down over following page content for 400 ms easing-out; then black Heading 2 links at vertical centre fade in over 400 ms. Email and two phones from `site_settings` are secondary links at the bottom with a 32 px viewport inset; mobile `320–767 px` header is fixed above scrolling content on every page with a white surface, green `logo-mob.svg` at 131×48 px, 48×48 px menu target, same flex row and equal 12 px top/bottom insets at its 393px-reference 72px height; with JavaScript a 400ms full-viewport overlay reveals its links, while the single admission CTA is anchored inside that fixed overlay 16px from its bottom at the 393px reference and shares their left inset; mobile overlay links use the Menu role (24px, 500, line-height 1, 0px tracking at 393px) with a 12px vertical gap at the 393px reference, scaled with S_m; Home achievements mobile rows recompose into a three-level record: Year and Choir, Achievement, then Competition (4px below Result, Body); default/hover/focus-visible/active/current-page; semantic primary navigation and admission CTA | JavaScript only enhances the server-rendered controls; navigation, CTA and contacts remain available without it |

Home mobile clarification: on the initial Home Hero only, the shared Sidebar is transparent and static over the video; white logo is `131×48 px` at `12 px` left / `24 px` top and its menu target is `48×48 px` at `12 px` right. After 100 px scroll enhancement it becomes the existing fixed white header. The server-rendered navigation remains available without JavaScript.

NewsCard tablet clarification: at the `1279 px` tablet reference, the category Badge remains `64×64 px` and its SVG glyph is `24×24 px`, equal to the desktop reference; both follow the shared `Sₜ` projection down to `768 px`.


Уточнение tablet Sidebar: визуальная поверхность menu-кнопки имеет `64×64 px` на reference `1279px` и непрерывно следует `Sₜ` до `38.43×38.43 px` на 768px; интерактивная область остаётся центрированным минимумом `48×48 px`. VK и Telegram используют свой отдельный Footer-derived размер.
Уточнение Sidebar для tablet/mobile заменяет прежний нижний блок контактов: на tablet (`768–1279 px`) в нижнем левом углу overlay остаются только supplied SVG-ссылки VK и Telegram (`48×48 px` на reference 1279px, gap `4px`); на mobile (`320–767 px`) они занимают левую часть нижнего ряда (`48×48 px`, gap `4px` на reference 393px), а CTA начинается через `48px` и тянется до правого content inset. Значения следуют `Sₜ`/`Sₘ`, с minimum target `48px`.
| `Footer` `4641:3638` | `layout/footer.php` — реализован | reference 1920 px: 1616×469 px; desktop/tablet replace the Footer address with supplied VK and Telegram link icons, 48×48 px at references 1920/1279 and top-aligned to «О капелле»; tablet 768–1279 px: natural-height two-column flow with 48px padding on all sides at the 1279px reference, following S_t; its navigation-group, contact-link and social-link gaps are component-owned and project from 40px, 8px and 4px at the reference; its 24px column and 8px list gaps are component-owned as well; mobile 320–767 px: full-bleed 393px-reference composition with 24px top corners/inset, a Heading 2 slogan, navigation-and-contacts columns 94px / 79px / 131.5px, 48px social links and visible phones/email; the address is visually replaced; default/hover/focus-visible/active; navigation, dynamic contacts, privacy link | shared layout only; no page-specific Footer variants |
| `Legal document` | `page-politika-konfidencialnosti.php` — planned | isolated legal page without Sidebar or Footer; one semantic H1, ordered sections and lists; content column uses existing Heading 1/Heading 3/Body roles and the common S, Sₜ and Sₘ projections; no new visual token or component variant | long text, links, 1280/1279/768/767/320 px overflow |


Уточнение Footer tablet: VK и Telegram имеют `48×48 px` на reference `1279px` и следуют общей шкале `Sₜ`; desktop и mobile не меняются.
## Desktop fluid-scale contract

В диапазоне `1280–2560 px` страница и повторяемые компоненты используют единый коэффициент `S`: `66.6667%` на `1280 px`, `100%` на `1920 px`, `133.3333%` на `2560 px`. Страница распределяет ширину и позицию компонента; ratio, типографика, padding, gaps, радиусы, изображения и прочая внутренняя геометрия принадлежат компоненту и масштабируются тем же `S`.

| Компонент | Геометрический контракт desktop |
|---|---|
| NewsCard | Референс `365×270 px` (`22.8125rem × 16.875rem`) на `1920 px`; сохраняет owned ratio `365/270` во всех desktop-композициях, page-level `aspect-ratio: auto` и независимая высота запрещены; desktop-сетка главной выделяет каждой карточке ровно `--news-card-width` и центрирует двухкарточный ряд |
| Advantage | Референс `388×170` на `1920 px`; оболочка и внутренности используют один `S` |
| MediaCard Big / Small | Референсы `797×540` и `390×540`; новый page-specific ratio сначала оформляется как явный вариант компонента |
| ApplicationForm | Поверхность, поля, gaps, action и success-состояние масштабируются согласованно; родитель не задаёт отдельную кривую высоты |
| DataTable | Типографика, padding и minimum row height масштабируются согласованно либо строка получает natural height |
| Sidebar / Footer | Shell использует ту же шкалу, что editorial canvas; фиксированные offsets не создают отдельную геометрию |

Для page-секций «О капелле» desktop-контракт также обязателен: «Жизнь капеллы» использует `803×600 px` на колонку и ratio `803/600`; тёмная текстовая карточка имеет равный outer padding `32 px` на desktop reference 1920 через `--about-life-card-padding`; пара Badge + icon в верхней части слайда размещается без gap, «Педагоги» — page-owned TeacherCard `257×340 px` с ratio `257/340`; фотография `160 px` и отступ до имени `71 px` следуют общему `S`. На mobile `320–767 px` «Жизнь капеллы» переключается в stacked-вариант: inverse text card и фото имеют высоту `460 px` на reference `393 px` через `--about-life-mobile-column-height`; card использует `--about-life-mobile-card-padding` для `16 px` padding; заголовок использует `--about-life-mobile-title-inset` для верхнего inset `24 px`, а текстовый контейнер — `--about-life-mobile-copy-width` для ширины `310 px`. Оба значения следуют `Sₘ`; описания каждого слайда — один `.orlyata-about__life-copy.type-body` с семантическими абзацами, разделёнными `--body-paragraph-spacing-mobile` (`8 px`). TeacherCard содержит только переданный локальный портрет и не выводит заглушку. Эти секции остаются page-owned и не становятся универсальным page-level паттерном.

Обязательные page-level baselines: `1280`, `1920`, `2560 px`; smoke: `1440`, `1600`, `2240 px`. Тесты проверяют ratio и связанную геометрию на реальной странице, а не только в изолированной story.

## Композиционные секции

Секции принадлежат страницам, но собираются из общих компонентов. Footer и Sidebar — единые site-wide компоненты. Их геометрия, данные и states принадлежат компонентам: страницы не создают wrapper-классы или CSS-селекторы для их использования, не переопределяют Footer (ширину, aspect ratio, padding, центрирование, порядок контента или контакты) и не создают page-specific варианты. Для tablet-страницы с тем же shell обязательна та же документированная token projection, что у принятого референса; на 1279 и 768 px проверяются computed rectangles Sidebar/Footer/canvas и геометрия общих DataTable/Card. Расхождение устраняется в общем shell или компоненте, не локальным CSS страницы. Разрешены только документированные элементы BEM и утверждённые модификаторы самого компонента; для Sidebar меняются лишь реальные ссылки и состояние текущего маршрута (`aria-current="page"`). Внешний отступ Footer принадлежит компоненту и применяется один раз вне page-grid: `120px` на reference `1920px` с общей desktop-шкалой `S`.

- Hero;
- News preview/list;
- News category filters: page composition reuses Button primary/secondary as server-rendered links; at 1920 px the group begins 64 px below PageHero through `--news-filter-offset-block` and follows desktop S; tablet/mobile use their `Sₜ`/`Sₘ` projections and a natural-height wrapping list. Every internal page uses `--internal-page-mobile-hero-content-gap` after its Hero: 24 px at the 393 px mobile reference, projected with `Sₘ`.
- News list: filters and the DataTable are one page-owned reveal block; on viewport entry it reuses the first MediaGallery block’s opacity-only reveal with unchanged delay, duration and easing, and stays visible without JavaScript. The table begins below filters by `--news-filter-table-gap` (32 px at 1920), reuses DataTable `news`, and its desktop four-column allocation matches the photo/video DataTable variant; on tablet the four tracks and shared gap remain unchanged while only each body-row Name cell uses 90% of its original two-track area, and a multiline Name grows the row naturally with the same block padding and first-line top alignment as the desktop achievements Competition cell; mobile uses the shared labelled DataTable reading record and its page wrapper has the mobile-only `--archive-mobile-filter-table-gap` top margin (40px at 393px, following S_m). The server and client share its row-category hidden state, so multi-select filters work after a reload and without one; rows newly shown by a filter fade from opacity 0 to 1 over 800 ms.
- Notes library: filters, the server-rendered search form and DataTable `notes` are one page-owned opacity-only reveal block with the first MediaGallery block’s unchanged delay, duration and easing; without JavaScript it is visible. At 1920 px the table begins 32 px after the filter row through shared `--news-filter-table-gap`, and its download arrow rolls vertically. At mobile 320–767px a page-owned 36px Search toggle with a non-layout 48px transparent pointer area precedes the horizontal filters; its supplied search-md glyph is 16px with a 1.5px stroke, and the visible container ends --home-mobile-space-16 (16px at the 393px reference) before the filters, uses the same full radius as the archive-filter controls and is vertically aligned with them; the Notes filter scrollport ends flush with the viewport at inline end without changing the separate guttered DataTable; with JavaScript it opens the named `notes-search-fullscreen` panel over the mobile header: the 72px-at-393px fixed top bar opens instantly; --color-overlay-content-scrim darkens only the content below it by 20%. The 48px SearchInput starts from the left 12px gutter and receives immediate focus so text entry opens the keyboard. On toggle activation, the page scrolls without animation so filters begin 24px below the fixed bar. Its 48px close control follows after a 24px gap at the right gutter and centres a 16px 1.5px-stroke X; close, Escape and the scrim restore focus to the toggle. Without JavaScript the form remains in normal flow. Its mobile empty result reuses the 404 centered stack: --notes-empty-icon-size, Heading 2, Body secondary-on-muted and --space-4, with its own --notes-empty-offset-block after controls. The table starts after filters with the same `--archive-mobile-filter-table-gap` (40px at 393px, following S_m) as News, Photo and Video. Tablet and desktop form placement remain unchanged.
- Photo and video archives: filters and DataTable are one page-owned opacity-only reveal block with the first MediaGallery block’s unchanged delay, duration and easing; without JavaScript it is visible. At mobile the table begins after the filters through shared `--archive-mobile-filter-table-gap` (40px at the 393px reference, projected with `Sₘ`). Their shared server-rendered category filters use the News multiple-selection contract: categories are OR-matched, each selected category can be removed independently, and «Все» clears the selection. The comma-separated `category` URL preserves the no-JavaScript fallback and client-side History API; newly shown DataTable rows use the same 800 ms opacity reveal.
- Media preview;
- History;
- Choir groups;
- Teachers;
- Choir achievements;
- Application CTA | `template-parts/sections/application-cta.php` — реализован | one shared server-rendered semantic H2; it uses visual Heading 1 in three fixed lines at desktop/tablet and Heading 2 with inline text on mobile, plus a mobile full-bleed background surface that matches Footer while retaining ApplicationForm internal padding; page callers may set only the section/form IDs, form variant, submit label and page-owned reveal hook | approved CTA text, including the non-breaking space before «капеллу», and its container geometry are never duplicated in page templates |
- Achievements section | `template-parts/sections/achievements-table.php` + `assets/src/styles/components/achievements-table.css` — реализован | one shared server-rendered Heading 2, DataTable wrapper and `achievements` DataTable variant; the wrapper alone owns `--achievements-heading-content-gap` (24 px at 1920 desktop) on Home and About; callers supply their page-owned rows, section ID, optional continuation link and reveal hook | mobile row composition, table insets, typography and heading-to-table spacing are owned by the shared wrapper, never duplicated by a page |
- Score library/search;
- Photo gallery;
- Video embed;
- Contacts: page-owned rows reuse Sidebar, default PageHero and Footer; labels, contact values and social icons are server-rendered and stay available without JavaScript. The rows form one page-owned opacity-only reveal block using the first MediaGallery block’s unchanged delay, duration and easing; without JavaScript they remain visible. The 1920 px reference layout uses the global four-column grid, `64px` Hero-to-content gap, 1px separators and a `96px` reference trailing gap from the final text line to its following separator; only approved `--contacts-*` tokens control desktop scaling. Address is one text node without `<br>`; its `--contacts-address-inline-size` follows S/S_t/S_m so «КЦ «Зеленоград»» wraps naturally to the following line. Tablet `768–1279` keeps that four-track ownership unchanged and uses the same shared internal-page Sidebar/token scope as About (label 1; values/socials 3–4; descriptions 4), while its `64/96/24/48px` page rhythm follows the documented `S_t` projection from 1279px. Each tablet contact record is a fixed value / max-content, nowrap description pair, so descriptions such as «Общая электронная почта» do not introduce a page-specific fluid track. The shared --contacts-information-alignment-offset provides a 6px-at-1279 optical inset for address, values and social links, preserving their tracks and right edge. Mobile `320–767` uses the two-column `--contacts-mobile-label-track / --contacts-mobile-information-track` record (1fr / 2fr); address, connection pairs and social links occupy the information column and therefore share one vertical text line, and every textual role uses mobile Body typography. Social links reuse the shared Footer `--footer-social-size` (48px at the 393px reference) and its S_m scale. Sidebar, PageHero and Footer remain shared components; Contacts does not override their geometry.

Не следует превращать любую секцию в универсальный компонент. Новый общий компонент оправдан повторяемой структурой, поведением и устойчивым API минимум в двух контекстах.

## Контентные соответствия

| UI | Источник CMS |
| --- | --- |
| NewsCard и список новостей | `news` + закрытая taxonomy категории |
| Иконка категории | централизованная карта `category → icon`, не поле каждой записи |
| Teacher card/detail | `teacher` |
| Таблица достижений | `choir_achievement` |
| Таблица нот | `score` + choir filter |
| Фото-карточка/детальная | `photo_album` |
| Видео-карточка/детальная | `video` |
| Footer/Contacts | `site_settings` |
| ApplicationForm | `application` через защищённый endpoint |

## Правило расширения

Если макет требует варианта, которого нет в таблице:

1. проверить, нельзя ли решить задачу композицией существующих вариантов;
2. добавить вариант и его состояния в `manifest.json`;
3. обновить этот файл и tokens при необходимости;
4. только затем менять PHP/CSS/TypeScript;
5. проверить вариант во всех трёх responsive-режимах.
